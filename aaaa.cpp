#include <stdio.h>
#include <math.h>

int main(void)
{
	int sum = 0;
	
	sum = pow(2,0) + pow(2,1) + pow(2,2) + pow(2,3) + pow(2,4) + pow(2,5) + pow(2,6) + pow(2,7) + pow(2,8) + pow(2,9) + pow(2,10) + pow(2,11) + pow(2,12) + pow(2,13) + pow(2,14) + pow(2,15);



	printf("%d", sum);
	return 0;
}

